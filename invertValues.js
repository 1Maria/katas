function invert(array) {
  return array.map(element => (element * -1));
}

function invert(array) {
  return array.map(element => -element);
}

function invert(array) {
   return array.map(element => 0 - element);
}

invert([1,2,3,4,5]);
